name: Publish

on:
  repository_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Publish
        uses: cloudflare/wrangler-action@1.3.0
        with:
          apiToken: IeRPToWFYgx3vY2TdF7VqvCdYc0gNlbkT-wLCISd
        env:
          CF_ACCOUNT_ID: 958184040f7784c804ef08b835cbeb1e
